services:
  - type: web
    name: Aahara-AI-2
    runtime: python-3.11
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn phase7_backend.main:app --host 0.0.0.0 --port 8000
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
